'use client';
import Link from 'next/link'; import { Activity, BookOpen, HeartPulse, Home, LocateFixed, Menu, Phone, Settings, ShieldAlert, Siren, UserRound, UsersRound, X } from 'lucide-react'; import { useState } from 'react'; import { Logo } from './Logo'; import { LogoutButton } from './LogoutButton';
const nav=[['/dashboard','Overview',Home],['/contacts','Contacts',UsersRound],['/medical-id','Medical ID',HeartPulse],['/emergency','Emergency',Siren],['/profile','Profile',UserRound]] as const;
export function DashboardShell({children,userName}:{children:React.ReactNode;userName:string}){const [open,setOpen]=useState(false);return <div className="min-h-screen bg-[#050c16]"><aside className={`fixed inset-y-0 left-0 z-50 w-72 border-r border-white/10 bg-[#071426] p-5 transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between"><Logo compact/><button className="btn-ghost p-2 lg:hidden" onClick={()=>setOpen(false)} aria-label="Close menu"><X size={18}/></button></div><div className="mt-8 rounded-2xl bg-white/[.04] p-4"><p className="text-xs text-slate-500">SAFETY PROFILE</p><p className="mt-1 font-bold truncate">{userName||'Sahayak user'}</p><p className="mt-1 text-xs text-emerald-300">Private & protected</p></div><nav className="mt-7 space-y-1">{nav.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="flex items-center gap-3 rounded-2xl px-4 py-3 text-sm text-slate-300 hover:bg-white/5 hover:text-white"><Icon size={18}/>{label}</Link>)}</nav><div className="mt-8 border-t border-white/10 pt-5"><Link href="/profile" className="mb-2 flex items-center gap-3 rounded-2xl px-4 py-3 text-sm text-slate-400 hover:bg-white/5"><Settings size={18}/>Settings</Link><LogoutButton/></div></aside><div className="lg:pl-72"><header className="sticky top-0 z-40 border-b border-white/5 bg-[#050c16]/80 backdrop-blur-xl"><div className="flex h-16 items-center justify-between px-5 sm:px-8"><button className="btn-ghost p-2 lg:hidden" onClick={()=>setOpen(true)} aria-label="Open menu"><Menu size={19}/></button><div className="hidden items-center gap-2 text-xs text-slate-500 lg:flex"><Activity size={15} className="text-emerald-400"/> Safety workspace</div><div className="ml-auto flex items-center gap-3 text-sm text-slate-300"><ShieldAlert size={17} className="text-blue"/> {userName||'Your profile'}</div></div></header><main className="p-5 sm:p-8">{children}</main></div></div>}
