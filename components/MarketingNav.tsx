'use client';
import Link from 'next/link'; import { Menu, X } from 'lucide-react'; import { useState } from 'react'; import { Logo } from './Logo';
export function MarketingNav(){ const [open,setOpen]=useState(false); const links=[['Features','#features'],['How It Works','#how'],['Offline First','#offline']]; return <header className="fixed top-0 z-50 w-full border-b border-white/5 bg-[#050c16]/80 backdrop-blur-xl"><div className="shell flex h-20 items-center justify-between"><Logo/><nav className="hidden md:flex items-center gap-6 text-sm text-slate-300">{links.map(([t,h])=><a key={h} href={h} className="hover:text-white">{t}</a>)}<Link href="/hospital/login" className="hover:text-white">Hospital Portal</Link><Link href="/login" className="hover:text-white">Login</Link><Link href="/signup" className="btn-primary px-4 py-2.5">Get Started</Link></nav><button className="md:hidden btn-ghost px-3 py-2" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X size={20}/>:<Menu size={20}/>}</button></div>{open&&<div className="border-t border-white/10 bg-[#071426] md:hidden"><div className="shell flex flex-col gap-2 py-4">{links.map(([t,h])=><a key={h} href={h} onClick={()=>setOpen(false)} className="rounded-xl px-3 py-3 text-slate-300">{t}</a>)}<Link href="/hospital/login" className="rounded-xl px-3 py-3">Hospital Portal</Link><Link href="/login" className="rounded-xl px-3 py-3">Login</Link><Link href="/signup" className="btn-primary">Get Started</Link></div></div>}
}
